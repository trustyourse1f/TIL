SELECT
    TRUNCATE(PRICE/10000, 0)*10000 AS PRICE_GROUP,
    COUNT(TRUNCATE(PRICE/10000, 0)*10000) AS PRODUCTS
FROM PRODUCT
GROUP BY TRUNCATE(PRICE/10000, 0)*10000
ORDER BY TRUNCATE(PRICE/10000, 0)*10000